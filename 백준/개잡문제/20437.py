import sys

T = int(sys.stdin.readline().rstrip())

for _ in range(T):
    w = sys.stdin.readline().rstrip()
    k = int(sys.stdin.readline().rstrip())

    min_len = k
    long_len = 0
    sw1 = 1

    # 포함하니까 걍 ㄷ
    # 3번에 대한 로직



